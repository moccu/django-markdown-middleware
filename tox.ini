[tox]
env_list = py{311,312,313}-django52,py{312,313}-django60

[testenv]
runner = uv-venv-runner
dependency_groups = dev
setenv =
    PYTHONPATH = {toxinidir}
deps =
    django52: django>=5.2,<5.3
    django60: django>=6.0,<6.1
commands =
    pytest {posargs:--cov}
